
    function handleCredentialResponse(response) {
        console.log("Encoded JWT ID token: " + response.credential);
        // Bạn có thể giải mã JWT token này để lấy thông tin người dùng, hoặc gửi token này đến server để xác thực.
    }

